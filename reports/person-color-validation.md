# 南陈人物着色验收

2026-09-12。运行 `node scripts/validate-person-colors.mjs`：PASS。

验证直接调用生产代码的原表逐段渲染器和正文人物渲染函数。仅姓名及紧连姓名的爵号可带人物颜色；月份、官衔、任免说明均为黑色。复姓“司马消难”中的“司马”按姓名处理。

| 核验范围 | 覆盖结果 |
| --- | --- |
| 原表 A320:AB352 | 924 格，含表头与留空 AA 列；489 个非空格，4450 个字符逐字保留 |
| 原表姓名 | 152 位人物、657 次匹配；458 次非陈姓姓名全部为绿色 |
| 都督／刺史正文 | 586 条摘要、592 次姓名匹配；394 次非陈姓姓名全部为绿色 |
| 官职及说明 | 748 处“监、刺史、都督、不行、宣惠、持节、即位”等独立词组均为黑色；原表 1143 个原先有色的非姓名非空白字符改为黑色 |
| 原表字体和格式 | 924 格逐字符检查字体、字号、粗斜体、文字修饰、上下标和背景；保留原值，单元格 middle/top/bottom 对齐仍不误作文字基线 |
| 正文来源字体 | 510 次姓名有唯一可用的同年来源，全部保留对应原表姓名字体 |
| 宗室年度校验 | 1170 次父子关系与年度身份检查通过；在位皇帝及死亡次年以后不按同父兄弟补色 |

定点验证：583 年新蔡王陈叔齐、建安王陈叔卿即使本年原表未列，也按皇弟显示 `#FF0000`；584 年晋熙王陈叔文显示同色；现有嗣王承袭纪年沿用 `#7030A0`。559、566 年存在年内易帝，不作整年同父身份覆盖。

原表共 42 次姓名出现更新颜色，其中 10 次是原先全黑的姓名补色。陈姓姓名与原表 rich runs 的颜色冲突有 2 次：567 年 B331、568 年 B332 安成王陈顼由 `#BC6C64` 改为 `#D99694`（始封王、非当年皇弟皇子）。584 年 I348 的原格基础色虽是 `#C00000`，姓名 rich run 已为 `#FF0000`，现按姓名及年度身份渲染，不再把基础色套到整句。

验收中补入 13 个在真实单元格明确写出的姓名识别键：陈法武、陈秣、萧韶、萧泰、宁逵、吴世兴、黄偲、熊门超、鲁天念、顾觉、邓嵩、吕子廓、戴智烈。名单只用于识别人名，不增加任职或宗室关系。

575—576 年合州刺史“陈裒”此前没有来源色。现按用户既有封国考订，以及同一劾奏事件在[《陈书》卷二十九](https://zh.wikisource.org/zh-hant/陳書/卷29)作“陈裒”、[《南史》卷六十八](https://zh.wikisource.org/zh-hant/南史/卷68)作“陈褒”的异写，将显示身份对应到钟陵侯陈褒，参照原表 557 年 Z321 的宗室侯姓名色 `#984807`；原文字仍显示“陈裒”。未合并原表另写的“陈衮”，也未改动源考证。

限制：此脚本验证数据及 DOM 样式，不替代浏览器像素验收。正文其余 82 次姓名没有唯一可用的同年字体来源，继续使用正文默认字体；颜色依可核实身份或明确的异姓规则处理。无法确认宗室关系的陈姓人物（如陈稜、陈法武、陈秣）不凭姓氏推断为宗室。原 Excel 文件未改写；`data/chen-yearbook-formats.js`、`data/chen-governors.js`、`data/chen-fiefs.js` 相对 Git HEAD 无改动，运行前后源字符串、rich runs、封国和官员数据完全一致。

## 萧梁原表页的同类修复

共享原表脚本的梁分支也改为仅对明确姓名及紧连爵号着色，并为梁页加入既有 `data/liang-person-colors.js` 依赖。沿用其 140 人姓名／别名及年度色；缺年度色时只把本格原色套到已识别姓名。姓名别名混入的“正月、二月、十月”等日期前缀在匹配时去除。未扩充世系或修改历史数据；原文字、换行及各行之间的原有排版保留。

`node scripts/validate-liang-governor-yearbook.mjs` 已改为实际渲染验证并通过：502—556 年全表 1540 格（981 个非空格）、981 次明确姓名匹配，其中 903 次有来源色；391 处官衔／说明短语全部黑色。另有 3 个定点用例覆盖日期别名边界，以及 538 年扬州“宣城王大器”深红、539 年同人黄色。538 年同格的“正月、中军、长史”均为黑色。

梁页限制：姓名表未收录者，以及姓名虽已识别但年度色和本格原色均缺失者，继续黑色；不把任意官职短语猜作姓名。梁页暂无南陈格式侧录那样的逐字字体数据，仍沿用现有梁页字体排版。共享脚本更新后，南陈上述全部颜色验证再次通过。
