# 南陈原表格式恢复核查

2026-09-08。范围：南陈刺史年表的逐段渲染与正文人物格式映射。已只读提取用户保存的《7、表五、南朝刺史、長史、司馬年表.xlsx》Sheet1 南陈段原始 OOXML 格式；没有修改原 Excel 或历史数据。格式侧录一次生成，渲染收尾未重复导入。

- 格式数据 `meta.text_comparison`：864 格比较、864 格逐字一致，纯空白差异 0、正文差异 0、差异明细为空。`format_counts`：462 个非空格、126 个多色格、45 个含斜体格、0 个含下划线格。以上为既有生成数据的元信息，本次未重跑导入。
- `B326`：始兴王陈伯茂对应 `#C00000`，安成王陈顼对应 `#FF0000`；王质长史附注保留 9pt 斜体。人物文本切片保留各自原始颜色，不以同格首人颜色覆盖他人。
- 修正附录表头原先误取空白 `AA320` 的问题：现在按 `meta.appendix_column = AB` 取 `AB320`，正文仍取各年 `AB` 格；页面范围说明改为 `A321:AB352`，表头为 `A320:AB320`。AA 空列继续保留在格式数据中，不占州列。
- 原始文字逐段使用文本节点呈现，保留空格、换行、字色、字体、字号、粗斜体与文字修饰。普通文字的行内基线与单元格垂直对齐分开，字体的上下标仍保留。年表无填充格显示为白底，使固定表头不会透出滚动正文；定位与检索边框不覆盖原色。
- 本次仅运行一次轻量 Node VM/DOM 定点检查，结果 PASS：B326 逐段颜色及斜体字号、F321 粗体和双空格换行、AB320 表头与 AB321 附录、人物切片、定位检索下原色保持、合成样本的下划线取消及上标。南梁在同时加载南陈格式全局量的情况下，渲染 DOM 与 HEAD 版本完全一致。

本次检查没有执行全量测试或浏览器像素比对；下划线使用合成样本核验，因为该南陈原表格式数据没有下划线格。字体外观依赖客户端安装的宋体、黑体等原字体。

## 正文人物与封君

- 正文都督／刺史按同年、州列、姓名匹配原格中的具体字元，不再沿用“整格字体色归第一人”的旧映射；保留姓名原有字号、字体、粗斜体，不另外添加粗体或点状下划线。
- 现有爵号别名只用于连接既有人名，不改写原文字。对应来源不明确或同年格式有冲突时不猜配色，也不从别年借颜色。
- 一次映射定点检查确认：562 年揚州始興王伯茂为 B326 的 `#C00000`，安成王頊为 B326 的 `#FF0000`，侯安都为 C326 的 `#00B050`。
- 封君姓名和标签背景框使用同色系；多人不同色时各自着色。仅封君沿用“非陈姓绿色”的显示规则，不能覆盖都督／刺史原表颜色。郡县正文名称已去掉封君颜色覆盖，原来的政区、封国分类样式不变。
- 源工作簿未改写；任次投射、人工裁决、地方长官主表与史料内容未改动。
